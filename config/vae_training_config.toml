# VAE Training Configuration for frame-twin
# This config trains a VAE to compress 3D voxel structures

[metadata]
name = "lnp_vae_training"
description = "VAE training for LNP voxel structure compression"
random_seed = 42

[data]
voxel_library_path = "./output/lnp_5k/voxels.zarr"
split_strategy = "random"  # or "stratified"
train_ratio = 0.8
val_ratio = 0.1
test_ratio = 0.1
# stratify_params = ["shell1_radius_nm", "shell2_probability"]  # for stratified splitting

[model]
type = "vae"
input_channels = 9  # Number of material channels
latent_channels = 8  # Small latent space dimension
base_channels = 32  # Base number of channels (doubles each level)
levels = 3  # Number of downsampling levels (128³ → 8³ latent), 

[training]
device = "mps"  # or "cpu"
distributed = false  # Set true for DDP
num_epochs = 10
num_workers = 4
batch_size = 2
learning_rate = 2e-4
optimizer = "adam"  # "adam", "adamw", or "sgd"
scheduler = "cosine"  # "cosine", "step", or "none"

[loss]
reconstruction_weight = 1.0
kl_weight = 0.001

[checkpointing]
output_dir = "./output/lnp_5k/checkpoints/vae_5k_3levels"
save_every_epochs = 10
save_every_minutes = 60  # Time-based checkpointing
keep_last_n = 3
save_best = true  # Save best validation loss

[logging]
log_every_steps = 50
tensorboard_dir = "./output/lnp_5k/logs/vae_5k_3levels"
