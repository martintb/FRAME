[tool.uv.workspace]
members = ["packages/*", "apps/*"]

[tool.uv.sources]
frame-voxel = { workspace = true }
frame-geo = { workspace = true }
frame-twin = { workspace = true }

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
]
