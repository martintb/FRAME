[tool.uv.workspace]
members = ["packages/*", "apps/*"]

[tool.uv.sources]
frame-core = { workspace = true }
frame-geo = { workspace = true }
frame-twin = { workspace = true }
# frame-voxel = { workspace = true }  # Deprecated - replaced by frame-core

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
]
